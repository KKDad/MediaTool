source: /data/recordings
destination: /data/recordings_renamed

actions:
- org.westfield.action.RenameAction

renameAction:
  format: "{Show}/Season {Season}/{Show}-S{Season}E{Episode}-{Title}.{Format}"